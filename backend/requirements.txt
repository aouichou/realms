# Web Framework
fastapi>=0.136.3
uvicorn[standard]>=0.50.2
pydantic>=2.13.4
pydantic-settings>=2.14.1

# Mistral AI SDK
mistralai>=2.4.9,<3.0.0
tiktoken>=0.13.0  # Token counting for context management

# OpenAI SDK (used for OpenAI-compatible providers: Cerebras, Together, Sambanova)
openai>=2.41.0

# HTTP & Async
httpx>=0.28.1
aiofiles>=25.1.0

# Redis
redis>=8.0.0
hiredis>=3.4.0

# Cloud Storage (Cloudflare R2 via S3 API)
boto3>=1.43.24

# Database
asyncpg>=0.31.0
sqlalchemy>=2.0.50
alembic>=1.18.4
pgvector>=0.4.2  # pgvector Python client for vector embeddings

# Security
PyJWT>=2.13.0
bcrypt>=5.0.0
cryptography>=48.0.0
python-multipart>=0.0.32
email-validator>=2.3.0

# Observability
# Keep the OpenTelemetry core and contrib packages on the same release train
# to stay compatible with mistralai 2.4.x during dependency resolution.
opentelemetry-api>=1.43.0,<1.44.0
opentelemetry-sdk>=1.43.0,<1.44.0
opentelemetry-instrumentation-fastapi>=0.64b0,<0.65.0
opentelemetry-instrumentation-httpx>=0.64b0,<0.65.0
opentelemetry-instrumentation-redis>=0.64b0,<0.65.0
opentelemetry-instrumentation-sqlalchemy>=0.64b0,<0.65.0
opentelemetry-exporter-otlp>=1.43.0,<1.44.0
prometheus-client>=0.25.0

# Internationalization
babel>=2.18.0

# Semantic Similarity for Image Detection
-r requirements-transformers.txt

# Development & Testing
pytest>=9.1.0
pytest-asyncio>=1.4.0
pytest-cov>=7.1.0
httpx-sse>=0.4.3
aiosqlite>=0.22.1

# Code Quality
black>=26.5.1
ruff>=0.15.17
mypy>=2.1.0

# Utilities
python-dotenv>=1.2.2
